var first_id = $('.message').first().attr('data-id');
console.log(first_id);
if (first_id > 1) {
	$('.load-more').after('<%= escape_javascript(render @messages) %>');
	if ( first_id = $('.message').first().attr('data-id') == <%= @room.messages.first.id %> ){
		console.log(first_id);
		$('.loading-gif').remove();
		$('.load-more').remove();
	}

}